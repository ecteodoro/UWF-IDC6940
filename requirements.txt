# Minimal environment to run the notebooks (TFT + other models)
# Pins chosen to match your environment where available
torch==2.6.0
pytorch-lightning==2.5.1.post0
pytorch-forecasting==1.5.0

# Core data stack
numpy>=1.24
pandas>=2.0
matplotlib>=3.6
seaborn>=0.12
scikit-learn>=1.2

# Notebook / tooling
jupyter
ipykernel

# Optional / modeling extras (used across notebooks)
optuna>=3.0
lightgbm>=3.3
xgboost>=1.7

# For timeseries / stats models
statsmodels>=0.14
pmdarima>=2.0

# Convenience (plotting & evaluation helpers)
plotly
arviz

# Note: Install PyTorch from https://pytorch.org/get-started/locally for the appropriate
# CUDA/MPS/CPU build for your system. The pinned torch version above was the one used
# in the environment where these notebooks were developed.

